name: Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  quality:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        phpVersion:
          - '8.2'
        neosVersion:
          - '8.3'
          - '9.1'

    name: 'Quality - Neos ${{ matrix.neosVersion }}'

    steps:
      - name: Checkout package
        uses: actions/checkout@v3
        with:
          path: package

      - name: Checkout Neos base distribution
        uses: actions/checkout@v3
        with:
          repository: neos/neos-base-distribution
          ref: ${{ matrix.neosVersion }}
          path: neos-base-distribution

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.phpVersion }}
          tools: composer:v2, phpstan, phpunit

      - name: Get composer cache directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

      - name: Cache dependencies
        uses: actions/cache@v3
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ matrix.neosVersion }}-${{ hashFiles('neos-base-distribution/composer.json') }}
          restore-keys: ${{ runner.os }}-composer-${{ matrix.neosVersion }}-

      - name: Copy package into distribution
        run: |
          mkdir -p neos-base-distribution/Packages/Plugins
          cp -R "$GITHUB_WORKSPACE/package" neos-base-distribution/Packages/Plugins/Shel.Neos.ResourceImportPreprocessor

      - name: Install dependencies
        working-directory: neos-base-distribution
        run: |
          composer config --no-plugins allow-plugins.neos/composer-plugin true
          composer install --no-interaction --no-progress

      - name: Register package and regenerate autoloader
        working-directory: neos-base-distribution
        run: |
          composer config --no-plugins repositories.shel-crosscontentrepo path Packages/Plugins/Shel.Neos.ResourceImportPreprocessor
          composer require --no-interaction --no-progress shel/neos-resource-import-preprocessor

      - name: PHPStan analysis
        working-directory: neos-base-distribution/Packages/Plugins/Shel.Neos.ResourceImportPreprocessor
        run: phpstan analyse -c phpstan.neon --no-progress

      - name: PHPUnit tests
        working-directory: neos-base-distribution/Packages/Plugins/Shel.Neos.ResourceImportPreprocessor
        run: phpunit -c phpunit.xml
